import 'package:flutter/material.dart';

class DemandsTab extends StatelessWidget {
  const DemandsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        //title: const Text('Demands'),
      ),
      body: const Center(
        child: Text('Demands Tab'),
      ),
    );
  }
}
