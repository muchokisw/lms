import 'package:flutter/material.dart';

class IntellectualPropertyTab extends StatelessWidget {
  const IntellectualPropertyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        //title: const Text('Intellectual Property'),
      ),
      body: const Center(
        child: Text('Intellectual Property Tab'),
      ),
    );
  }
}
