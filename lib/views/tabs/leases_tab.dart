import 'package:flutter/material.dart';

class LeasesTab extends StatelessWidget {
  const LeasesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
       //title: const Text('Leases'),
      ),
      body: const Center(
        child: Text('Leases Tab'),
      ),
    );
  }
}
